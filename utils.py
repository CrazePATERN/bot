# Здесь можно разместить вспомогательные функции, например, для обработки строк или других операций.
# В вашем случае это могут быть функции, которые будут использоваться в других частях бота.

def is_valid_referral_id(referral_id):
    return len(referral_id) in range(8, 11) and referral_id.isdigit()